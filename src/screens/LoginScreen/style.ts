import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  input: {
    marginBottom: 16,
    borderColor: '#ccc',
    borderWidth: 1,
    padding: 8,
  },
  errorText: {
    color: 'red',
  },
});

export default styles;
